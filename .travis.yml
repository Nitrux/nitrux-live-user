dist: focal

before_install:
  - gem install package_cloud

script:
  - sudo ./gh-build.sh
  - package_cloud push nitrux/repo/debian/trixie *.deb

branches:
  only:
    - minimal

notifications:
  email: false
