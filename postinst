#! /bin/bash

set -x

echo "==> Adding user 'nitrux'."

groupadd sambashare
groupadd wheel
groupadd fuse

useradd -G adm,cdrom,dip,plugdev,bluetooth,lpadmin,sambashare,input,wheel,fuse,docker,pipewire -m nitrux

printf "%s\n" nitrux nitrux | passwd nitrux
